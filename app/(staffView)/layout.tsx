// This route group intentionally has no sidebar — used for the full-page staff detail view.
export default function StaffViewRootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
